'use strict';

/**
 * data-broadcast-beli router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::data-broadcast-beli.data-broadcast-beli');
