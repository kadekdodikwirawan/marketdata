'use strict';

/**
 * data-broadcast-beli service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::data-broadcast-beli.data-broadcast-beli');
