'use strict';

/**
 *  broadcast-data-beli controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::broadcast-data-beli.broadcast-data-beli');
