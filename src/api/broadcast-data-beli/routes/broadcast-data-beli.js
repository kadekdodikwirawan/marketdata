'use strict';

/**
 * broadcast-data-beli router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::broadcast-data-beli.broadcast-data-beli');
