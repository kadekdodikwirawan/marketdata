'use strict';

/**
 * broadcast-data-beli service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::broadcast-data-beli.broadcast-data-beli');
